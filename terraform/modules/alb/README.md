# ALB edge module

Creates the only public application entrypoint: a three-AZ HTTPS ALB, ACM certificate and DNS validation, Route53 alias, IP target group for private Kubernetes workloads, and AWS managed/rate-limiting WAF rules.
