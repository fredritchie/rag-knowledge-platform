# RDS module

Creates private Aurora PostgreSQL instances across the private subnets. Storage, Performance Insights, and the AWS-managed master credential are encrypted by a customer-managed KMS key. SSL is mandatory and the cluster exports PostgreSQL logs.
