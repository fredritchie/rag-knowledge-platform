export default function Ingestion(){return <><header><div className="eyebrow">Pipeline operations</div><h1>Ingestion status</h1><p>Track real processing stages from upload through activation.</p></header><section className="card" style={{marginTop:24}}><h2>Pipeline stages</h2><div className="pipeline" style={{marginTop:18}}>{[["RECEIVED",100],["PARSING",100],["CHUNKING",100],["EMBEDDING",74],["INDEXING",0],["VALIDATING",0]].map(([name,value])=><div className="stage" key={name}><b>{name}</b><div className="bar"><i style={{width:`${value}%`}}/></div><span>{value===100?"✓":value?`${value}%`:"pending"}</span></div>)}</div><p>Open a document detail page to see persisted job events for that version.</p></section></>}
